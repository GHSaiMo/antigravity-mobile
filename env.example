# ==============================================================================
# Antigravity Mobile Gateway - 环境配置文件示例 (env.example)
# ==============================================================================
# 使用说明：
# 1. 复制本文件并重命名为 .env：
#    cp env.example .env
# 2. 在下方填写你的 Bark 推送地址（填入后网关会自动开启 iOS 通知推送功能）
# ==============================================================================

# ------------------------------------------------------------------------------
# 🔔 iOS Bark 实时推送通知配置
# ------------------------------------------------------------------------------
# 【如何下载 Bark App】:
#   - 在 iPhone / iPad 的 App Store 搜索「Bark」
#   - 官方应用名: 「Bark - 给你的手机发推送」 (开发者: Feng Wu / 伍峰，开源作者 Finb)
#   - App Store 链接: https://apps.apple.com/cn/app/bark-%E7%BB%99%E4%BD%A0%E7%9A%84%E6%89%8B%E6%9C%BA%E5%8F%91%E6%8E%A8%E9%80%81/id1403753865
#   - 注意避坑: 请认准纯色极简图标，不要下载成带有小狗图标的国外儿童监控软件 "Bark - Parental Controls"
#
# 【如何获取与复制专属链接】:
#   1. 在 iPhone 上安装并打开 Bark App；
#   2. 首页顶部会自动生成你设备的专属服务器链接（形式如: https://api.day.app/YOUR_DEVICE_KEY/）；
#   3. 点击链接卡片或右上角复制图标，即可将完整链接或 Device Key 复制到剪贴板。
#
# 【开启说明】:
#   - 只要在下方 BARK_URL 填入复制的链接（或 Device Key），网关启动时就会自动启用实时通知！
#   - 若留空或注释掉此项，通知功能将自动保持静默关闭。
# ------------------------------------------------------------------------------
BARK_URL=https://api.day.app/YOUR_DEVICE_KEY/

# 【Antigravity 推送图标】
# 默认指向本项目开源仓库的 Antigravity 官方矢量高清图标。
# Bark 收到推送时会自动拉取此图标渲染在 iPhone 锁屏与通知栏（需 iOS 15+）。
BARK_ICON=https://raw.githubusercontent.com/GHSaiMo/antigravity-mobile/main/web/icons/icon-192.png

# 【通知分组名称】
# 在 iOS 通知中心将所有来自反重力 Agent 的提醒自动归类折叠为一组，避免多条消息霸屏。
BARK_GROUP=Antigravity

# 【需要审批 / Action 时的提示音】
# 常用提示音可选: alarm (警报长音), anticipation, bell, horn, multiwayinvitation
BARK_SOUND_ACTION=alarm

# 【任务全部执行完成时的提示音】
# 常用提示音可选: glass (清脆短促), paymentsuccess, success, chime
BARK_SOUND_COMPLETE=glass

# 【Bark 推送超时秒数】（单次 HTTP 请求超时，默认 10 秒）
# BARK_TIMEOUT_SECONDS=10

# ------------------------------------------------------------------------------
# 🌐 网关基础监听与网络配置 (可选)
# ------------------------------------------------------------------------------
# 网关监听端口，默认 58900
# GATEWAY_PORT=58900
# 绑定主机地址，留空绑定全部接口，也可指定 127.0.0.1 或内网物理 IP
# GATEWAY_HOST=

# ------------------------------------------------------------------------------
# 🔐 认证与安全配置
# ------------------------------------------------------------------------------
# 管理员特权密钥：用于跨网络管理设备（枚举设备、吊销设备、签发会话），请设置高强度随机字符串。
# 启用 FRP / 非 loopback 监听时必须设置，且只能通过 Authorization: Bearer 传递（禁止 query）。
# ADMIN_TOKEN=your_super_secret_admin_token_here

# 设备认证凭证持久化文件路径（默认 ~/.antigravity_mobile_auth.json）
# AUTH_STORE_PATH=~/.antigravity_mobile_auth.json

# 是否完全禁用本地回环鉴权。仅当网关只监听 127.0.0.1 且未启用 FRP 时生效，否则进程拒绝启动。
# AUTH_DISABLED=0

# 手机蜂窝 IPv6 直连配对（默认关，P0 安全默认不把全球单播 IPv6 写进二维码）
# 打开后：
#   1. 网关继续 dual-stack 监听（即使开了 FRP 也不会绑死 127.0.0.1）
#   2. 配对二维码 / pair 响应带上本机公网 IPv6
#   3. 手机在蜂窝网下优先走 http://[IPv6]:58900（需运营商给终端公网 IPv6）
# 没有 TLS 证书时这是明文 HTTP，靠一次性配对码 + 设备 token 保护，请勿把端口映射给无关网络。
# INCLUDE_PUBLIC_IPV6=1

# 跨域与 WebSocket 允许的 Origin 域名列表（逗号分隔，如 https://app.example.com,http://192.168.1.50:58900）
# ALLOWED_ORIGINS=

# 文件读取白名单（冒号分隔）。未设置时默认允许用户主目录（仍拦截密钥、.env、Application Support 等）。
# 设置后只保留 ~/.gemini/antigravity 加上列出的根目录，用于收紧范围。
# ALLOWED_WORKSPACE_ROOTS=/Users/your_username/Projects:/Users/your_username/Workspace

# ------------------------------------------------------------------------------
# 🌍 外网直连与 DDNS / TLS 配置 (可选)
# ------------------------------------------------------------------------------
# 方案 1（灰云 DNS + 网关 HTTPS）
# DDNS 域名。开启 GATEWAY_SSL 时必须是证书 CN/SAN（不要填公网 IP）
# DDNS_HOST=agy.example.com

# 配对与中继 URL 使用 https://（必须同时提供下面两份证书，否则进程拒绝启动）
# GATEWAY_SSL=1

# Mac 本机 TLS 证书（Let's Encrypt fullchain + 私钥）。不要提交 git。
# TLS_CERT_FILE=/Users/your_username/.antigravity-mobile/certs/agy.example.com.fullchain.cer
# TLS_KEY_FILE=/Users/your_username/.antigravity-mobile/certs/agy.example.com.key

# ------------------------------------------------------------------------------
# ☁️ FRP 内网穿透隧道配置 (可选)
# ------------------------------------------------------------------------------
# 是否开启 FRP 隧道穿透 (0 或 1)
# FRP_ENABLE=0
# FRP 服务端地址与通信端口
# FRP_SERVER_ADDR=120.x.x.x
# FRP_SERVER_PORT=7000
# FRP 鉴权 Token (推荐生成 32 字节高熵随机密钥: openssl rand -hex 32)
# FRP_TOKEN=your_frp_auth_token
# FRP 映射的公网外部端口
# FRP_REMOTE_PORT=58900
# FRP 传输层是否启用 TLS 加密（默认 1 / true，推荐保持开启）
# FRP_TLS_ENABLE=1

# ------------------------------------------------------------------------------
# 🧪 Cockpit 与高级调试配置 (可选)
# ------------------------------------------------------------------------------
# Cockpit 配额数据目录覆盖（默认自动检测本地 Antigravity 数据目录）
# COCKPIT_DATA_DIR=/path/to/custom/cockpit/data

